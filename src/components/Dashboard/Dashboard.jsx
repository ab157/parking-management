const Dashboard = () => {
  return <div>Dashboard</div>;
};

export default Dashboard;

/*
    if admin => Data Table with all tickets sort in recent to oldest and with filter
    Ticket should be editable
    Table primary action => Create new Ticket
    onClick Ticket shows ticket details modal
*/
